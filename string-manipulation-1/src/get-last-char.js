/* exported getLastChar */
// create a variable to store the lastIndex
// the variables value should be the string.length -1 which assigns the lastIndex of the string to the variable
// return the string and pass in the lastIndex variable as its index
function getLastChar(string) {
  var lastIndex = string.length - 1;
  return string[lastIndex];
}
