/* exported getValue */
// create a new variable and set its values to them object at its key value
// return the object variable
function getValue(object, key) {
  var Object = object[key];
  return Object;
}
