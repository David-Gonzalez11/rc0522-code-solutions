/* exported lastChars */
// return the string with the slice method and give it a negative index to get the last characters of the string
function lastChars(length, string) {
  return string.slice(-length);
}
