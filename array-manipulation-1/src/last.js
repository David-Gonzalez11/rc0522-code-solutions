/* exported last */
// create a variable that holds the last index of the array
// return the array with the created variable passed as its index
function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}
